<h1>Questions:</h1>

<ol>
  <% @questions.each do |q| %>
    <li>
      <h2><%= link_to(q.title, question_path(q)) %></h2>
      <ul>
        <li><p><%= q.description %></p></li>
      </ul>
    </li>
  <% end %>
</ol>
